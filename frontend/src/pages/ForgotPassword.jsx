import { useState } from "react"; 
import { Link } from "react-router-dom"; 
import { forgotPassword,resetPassword } from "../services/api";
export default function ForgotPassword()
{const [email,setEmail]=useState("");const [newPassword,setNewPassword]=useState("");const [confirm,setConfirm]=useState("");const [verified,setVerified]=useState(false);const [message,setMessage]=useState("");const [error,setError]=useState("");async function verify(e){e.preventDefault();setError("");try{const r=await forgotPassword(email);setMessage(r.message);setVerified(true)}catch(err){setError(err.message)}}async function reset(e){e.preventDefault();setError("");try{const r=await resetPassword({email,new_password:newPassword,confirm_password:confirm});setMessage(r.message);setVerified(false)}catch(err){setError(err.message)}}return <div className="auth-page"><div className="auth-card"><div className="auth-logo">AI</div><h1>Password recovery</h1><p className="auth-subtitle">Use the existing backend recovery flow.</p>{error&&<div className="error-message">{error}</div>}{message&&<div className="success-message">{message}</div>}{!verified?<form onSubmit={verify}><div className="form-group"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><button className="primary-button">Verify email</button></form>:<form onSubmit={reset}><div className="form-group"><label>New password</label><input type="password" value={newPassword} onChange={e=>setNewPassword(e.target.value)} required minLength={8}/></div><div className="form-group"><label>Confirm password</label><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required minLength={8}/></div><button className="primary-button">Reset password</button></form>}<div className="auth-footer"><Link to="/login">Back to sign in</Link></div></div></div>}
