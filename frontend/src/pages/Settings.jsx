import {useState} from "react"; import PageHeader from "../components/PageHeader"; import {API_BASE_URL} from "../services/api"; import {useAuth} from "../context/AuthContext";
export default function Settings(){const {user}=useAuth();const [saved,setSaved]=useState(false);function save(){setSaved(true);setTimeout(()=>setSaved(false),1800)}return <><PageHeader title="Settings" description="Application and session information."/><div className="two-col"><div className="page-card"><h3>Environment</h3><div className="detail-grid"><div><label>API base URL</label><strong>{API_BASE_URL}</strong></div><div><label>Frontend</label><strong>React + Vite</strong></div><div><label>Backend</label><strong>FastAPI</strong></div><div><label>Real-time</label><strong>WebSocket</strong></div></div><div className="page-actions"><button className="btn-primary" onClick={save}>Save preferences</button></div>{saved&&<div className="success-message" style={{marginTop:15}}>Preferences saved.</div>}</div><div className="page-card"><h3>Current session</h3><p className="muted">Authentication is handled by the existing JWT backend. The frontend stores only the returned access token and current user profile.</p><pre className="muted">{JSON.stringify(user,null,2)}</pre></div></div></>}
