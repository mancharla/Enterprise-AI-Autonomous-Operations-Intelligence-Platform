import {useEffect,useState} from "react"; import PageHeader from "../components/PageHeader"; import {getOrganization,getOrganizationUsers} from "../services/api";
export default function Organization(){const [org,setOrg]=useState(null),[users,setUsers]=useState([]),[error,setError]=useState("");useEffect(()=>{Promise.all([getOrganization(),getOrganizationUsers()]).then(([o,u])=>{setOrg(o);setUsers(u)}).catch(e=>setError(e.message))},[]);return <><PageHeader title="Organization" description="Tenant identity, users and role-based access context."/>{error&&<div className="error-message">{error}</div>}{org&&<div className="stats-grid"><div className="stat-card"><div className="stat-label">Organization</div><div className="stat-value" style={{fontSize:18}}>{org.name}</div></div><div className="stat-card"><div className="stat-label">Code</div><div className="stat-value" style={{fontSize:18}}>{org.code}</div></div><div className="stat-card"><div className="stat-label">Tenant ID</div><div className="stat-value">#{org.id}</div></div><div className="stat-card"><div className="stat-label">Status</div><div className="stat-value">{org.is_active?'Active':'Inactive'}</div></div></div>}<div className="page-card"><h3>Organization users</h3><div className="table-wrap"><table className="data-table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.full_name}</td><td>{u.email}</td><td><span className="badge">{u.role}</span></td><td>{u.is_active?'Active':'Inactive'}</td></tr>)}</tbody></table></div></div></>}
